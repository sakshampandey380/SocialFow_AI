def normalize_platform(platform: str) -> str:
    return platform.strip().lower().replace("/x", "").replace(" ", "")
def normalize_platform(platform: str) -> str:
    return platform.strip().lower().replace("/x", "").replace(" ", "")
